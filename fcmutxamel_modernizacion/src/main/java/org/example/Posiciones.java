package org.example;
/**
 * Enum que enseña las posiciones que puede tener un jugador
 * dentro del equipo.
 * Sirve para evitar errores al escribir
 * las posiciones.
 */
public enum Posiciones {
    PORTERO,
    DEFENSA,
    CENTROCAMPISTA,
    DELANTERO
}
